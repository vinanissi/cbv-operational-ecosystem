# Scripts — CBV

Script hỗ trợ clasp, git, triển khai Apps Script, xuất báo cáo.

## Thư mục

- `clasp/` — đăng nhập, push/pull Apps Script.
- `git/` — helper nhánh, tag, hook (nếu có).
- `appscript-deploy/` — pipeline deploy.
- `report-export/` — xuất report ra định dạng nội bộ.
