# Config — CBV

Cấu hình mẫu, module, template cài đặt, gói kết nối. **Không** commit secret thật.

## Thư mục

- `env/` — mẫu biến môi trường.
- `modules/` — định nghĩa module.
- `install-templates/` — template triển khai.
- `connection-packages/` — gói kết nối (schema, không chứa password).
