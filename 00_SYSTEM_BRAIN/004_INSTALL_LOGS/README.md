# 004_INSTALL_LOGS — Log cài đặt

Lưu log các lần **cài đặt / triển khai** module, script, hoặc cấu hình.

## Quy ước

- Append-only: mỗi lần cài thêm một mục hoặc một file log mới.
- Ghi môi trường (dev/staging/prod), người thực hiện, và kết quả.
